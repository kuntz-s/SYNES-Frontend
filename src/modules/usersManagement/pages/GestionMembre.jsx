import React, { useEffect } from 'react';
import { useDispatch } from 'react-redux';

const GestionMembre = () => {
    const dispatch = useDispatch();
   /*  useEffect(() => {
        conosl
        dispatch(resetRoles())
    },[]) */
  return (
    <div>GestionMembre</div>
  )
}

export default GestionMembre